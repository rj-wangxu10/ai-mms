# AI 费用管理平台需求分析文档（MVP）

**文档版本**：V1.0  
**编制日期**：2026-08-13  
**目标读者**：产品、设计、开发、测试、业务负责人  

---

## 1. 项目背景与目标

### 1.1 业务痛点
- 公司使用 8 款 AI 工具/模型：飞书 AI、WorkBuddy/CodeBuddy、Codex、GitHub Copilot、Trae、Claude Code、UniAPI、FastGPT。
- 计费方式多样：席位月租、积分/token 用量、套餐内/外阶梯价、人民币/美元混合结算。
- 费用数据分散在各工具后台、Excel 台账、邮件审批中，人工对账效率低、管控滞后。
- 三类角色（员工、部门主管、预算管理员）无法实时掌握预算消耗、剩余额度、超支风险。

### 1.2 项目目标
构建统一的 AI 费用管理平台 MVP，实现：
1. **消费集中归集**：自动/半自动汇总各 AI 工具的消费、额度、预算数据。
2. **额度闭环管控**：从预算拆分 → 部门额度 → 个人额度 → 消费扣减 → 超额预警 → 追加申请 → 审批，全流程可追踪。
3. **分层视图**：为三类角色提供与职责匹配的数据视图与操作入口。
4. **可验证、可扩展**：需求自洽、数据模型清晰，支撑后续接入更多工具或复杂计费规则。

---

## 2. 用户角色与权限

| 角色 | 核心职责 | 数据可见范围 | 关键操作 |
|---|---|---|---|
| 员工（Employee） | 日常工作使用 AI 工具 | 个人当月/历史消费、个人剩余额度 | 查看消费明细、申请个人额度/追加额度 |
| 部门主管（Dept Manager） | 管理部门预算与成员使用 | 本部门预算、已用、剩余、成员排名、模型分布 | 审批本部门额度申请、查看分析报表、导出对账 |
| 预算管理员（Budget Admin） | 统筹全公司 AI 预算 | 全公司总预算、各部门使用率、模型费用排行、超预算预警 | 配置预算/工具/费率、审批跨部门/大额申请、设置预警规则、导出全局报表 |
| 系统（System） | 自动同步、计算、预警 | 全量数据 | 定时同步工具账单、触发预警、生成统计 |

> **说明**：本 MVP 不引入复杂的组织架构管理，默认公司与部门两级结构；角色由预算管理员在后台配置。

---

## 3. 功能需求

### 3.1 功能全景图（按角色）

```
┌─────────────────────────────────────────────────────────────┐
│                    预算管理员（全局视角）                      │
│  预算配置 → 工具/模型/费率管理 → 额度模板 → 数据同步 → 全局看板   │
│       ↓                                                       │
│  部门预算分配 ───────────────────────────→ 部门使用率监控/预警   │
└─────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────┐
│                    部门主管（部门视角）                        │
│  部门预算/剩余 → 模型分布 → 成员排名 → 额度申请审批 → 部门报表   │
│       ↓                                                       │
│  个人额度分配 ───────────────────────────→ 部门内预警/通知     │
└─────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────┐
│                      员工（个人视角）                          │
│  本月消费总览 → 各工具花费 → 剩余额度 → 消费明细 → 额度申请      │
└─────────────────────────────────────────────────────────────┘
```

### 3.2 员工功能

#### 3.2.1 个人消费总览
- 展示本月已消费金额、剩余可用额度、预算上限。
- 支持切换月份查看历史。
- 展示消费进度条，颜色区分：安全（<80%）、警戒（80%-100%）、超支（>100%）。

#### 3.2.2 工具消费明细
- 按 AI 工具/模型分组展示本月消费金额、用量（token/积分/席位数）。
- 支持查看单笔消费记录：时间、工具、模型、用量、金额、用途备注（如工单号）。
- 支持按工具/时间范围筛选与导出 CSV。

#### 3.2.3 剩余额度查询
- 实时展示个人可用额度。
- 展示额度来源：月度基础额度 + 已审批追加额度。
- 展示额度有效期（自然月清零/季度结转/项目周期，由管理员配置）。

#### 3.2.4 额度申请与追加
- **普通申请**：员工填写申请额度、用途说明、使用工具/模型、预计周期，提交给直属部门主管审批。
- **追加申请**：当月额度不足时，一键发起追加申请，系统自动带入当前消费与剩余数据。
- 申请进度可追踪：待审批 / 已通过 / 已驳回，并显示审批意见。

#### 3.2.5 预警通知
- 当月消费达到额度 80% 时，系统推送站内消息/邮件提醒。
- 额度耗尽时，再次提醒并提示可发起追加申请。

### 3.3 部门主管功能

#### 3.3.1 部门预算看板
- 展示部门总预算、已用金额、剩余金额、预算使用率。
- 支持按月份/季度/年度切换。
- 超预算或达到阈值时，标红预警。

#### 3.3.2 模型分布分析
- 展示本部门各 AI 工具/模型的费用占比（饼图/柱状图）。
- 展示各工具用量趋势（折线图）。
- 识别高消费工具，辅助主管做采购/续约决策。

#### 3.3.3 成员消费排名
- 展示部门成员消费金额排名、用量排名。
- 支持下钻查看某成员的消费明细与额度使用情况。
- 支持导出部门成员对账单。

#### 3.3.4 额度申请审批
- 审批列表：待审批、已通过、已驳回。
- 审批操作：通过 / 驳回（需填写理由）。
- 审批约束：
  - 通过额度不得超过部门剩余可用预算。
  - 超过一定阈值（如 5000 元或 50% 部门剩余预算）需升级至预算管理员审批。
- 审批结果实时通知申请人，并自动更新个人可用额度。

#### 3.3.5 部门额度分配（给下属员工）
- 主管可将部门预算拆分为员工月度基础额度。
- 支持批量导入/模板分配。
- 分配后部门剩余预算实时扣减。

### 3.4 预算管理员功能

#### 3.4.1 公司预算配置
- 配置公司年度/月度 AI 总预算，支持人民币与美元分别设置汇率。
- 将总预算拆分至各部门，支持按比例/固定金额分配。
- 预算调整记录留痕。

#### 3.4.2 工具与模型管理
- 维护 AI 工具基础信息：名称、类型、计费方式（席位/用量/混合）、币种、供应商。
- 维护模型/套餐与费率：模型名、单价、套餐阈值、阶梯价、结算币种。
- 支持配置工具的数据同步方式：API 自动同步、文件导入、手动录入。

#### 3.4.3 数据同步与对账
- **自动同步**：通过工具开放 API 或账单文件定时拉取消费数据。
- **手动导入**：支持 Excel/CSV 批量导入账单。
- **对账校验**：系统汇总金额与导入账单总额比对，差异标红。
- **消费归一化**：将 token、积分、席位等折算为统一货币金额。

#### 3.4.4 全局看板
- 公司总预算、总已用、总剩余、整体使用率。
- 各部门预算使用率排名/对比。
- 全公司模型费用排行榜。
- 超预算部门/成员高亮展示。

#### 3.4.5 超预算预警
- 设置多级预警阈值（如 80%、100%、120%）。
- 触发方式：部门预算使用率、公司总预算使用率、个人额度使用率。
- 通知对象：相关人员（员工、主管、预算管理员）。
- 通知渠道：站内消息、邮件（MVP 阶段）。

#### 3.4.6 额度申请终审
- 对大额或跨部门额度申请进行终审。
- 可调整申请金额后通过。
- 审批记录留痕，支持审计。

#### 3.4.7 全局报表与导出
- 导出公司/部门/个人维度的费用报表。
- 导出模型费用排行、预算执行率报表。
- 支持 PDF/Excel 格式。

---

## 4. 功能操作链（完整闭环）

### 4.1 预算到消费的闭环

```
[预算管理员] 配置公司年度总预算
        │
        ▼
[预算管理员] 拆分到各部门月度预算
        │
        ▼
[部门主管] 将部门预算拆分为员工月度基础额度
        │
        ▼
[系统] 定时同步各 AI 工具账单 → 生成消费记录
        │
        ▼
[系统] 按员工/工具/模型聚合消费金额，扣减个人额度
        │
        ▼
[系统] 触发额度/预算预警（80%/100%/120%）
        │
        ▼
[员工] 收到预警 → 查看消费明细 → 发起追加申请
        │
        ▼
[部门主管] 审批追加申请（小额）/ 升级预算管理员（大额）
        │
        ▼
[系统] 审批通过后增加个人额度，并扣减部门剩余预算
        │
        ▼
[预算管理员/部门主管] 月末查看预算执行率报表，指导下月预算调整
```

### 4.2 申请审批闭环

```
[员工] 提交额度申请（基础额度/追加额度）
        │
        ▼
[系统] 校验申请金额 ≤ 部门剩余预算；否则标记为需升级审批
        │
        ▼
[部门主管] 审批（通过/驳回）
        │  ├── 通过 → 更新个人额度，通知员工
        │  └── 驳回 → 填写理由，通知员工
        │
        ▼（超过阈值）
[预算管理员] 终审
        │
        ▼
[系统] 记录审批流水，生成审计日志
```

### 4.3 数据同步与对账闭环

```
[系统] 定时任务调用各工具 API / 读取账单文件
        │
        ▼
[系统] 解析原始账单 → 映射到统一消费记录（工具、模型、用量、金额、币种、用户）
        │
        ▼
[系统] 按自然月聚合，与个人额度、部门预算、公司总预算比对
        │
        ▼
[系统] 生成对账差异报告，异常数据标红
        │
        ▼
[预算管理员] 核对差异 → 修正工具配置或补录数据
        │
        ▼
[系统] 重新计算消费与剩余额度，更新所有视图
```

---

## 5. 业务规则与约束

### 5.1 预算与额度规则
1. **预算层级**：公司预算 ≥ 各部门预算之和；部门预算 ≥ 部门内员工基础额度之和。
2. **币种统一**：所有消费金额按配置汇率统一折算为人民币（或公司本位币）进行汇总与预警。
3. **月度清零**：MVP 阶段个人月度基础额度自然月清零；追加额度仅限当月有效。
4. **额度扣减顺序**：先扣减月度基础额度，再扣减追加额度。
5. **超额控制**：MVP 阶段采用“事后预警 + 审批追加”模式，不阻断工具实际使用（因多数工具无法实时拦截）。

### 5.2 审批规则
1. 员工额度申请金额 ≤ 部门剩余预算时，由部门主管审批。
2. 员工额度申请金额 > 部门剩余预算，或单笔超过设定阈值时，升级至预算管理员终审。
3. 驳回的申请需填写理由，员工可重新提交。
4. 已通过申请不可撤回，除非预算管理员在后台调整。

### 5.3 数据同步规则
1. 同步周期：支持日级/小时级/手动触发。
2. 账单日期以工具侧出账时间为准。
3. 若工具未提供用户级明细，则按部门公共池分摊或标记为未分配，由预算管理员人工归属。
4. 同步失败需记录日志并通知管理员。

### 5.4 预警规则
1. 个人额度使用率达 80% 通知员工；达 100% 再次通知并抄送主管。
2. 部门预算使用率达 80% 通知部门主管；达 100% 通知主管与预算管理员。
3. 公司总预算使用率达 80% 通知预算管理员；达 100% 通知管理层（MVP 阶段可配置邮件列表）。

---

## 6. 数据逻辑模型（概念模型）

### 6.1 实体关系图（ER）

```
┌──────────────┐       ┌──────────────┐       ┌──────────────┐
│   Company    │1────N │  Department  │1────N │     User     │
│  公司/预算     │       │   部门/预算    │       │  员工/角色    │
└──────────────┘       └──────────────┘       └──────┬───────┘
                                                     │
                                                     │1
                                                     │
                              ┌──────────────────────┘N
                              ▼
                       ┌──────────────┐
                       │ QuotaRecord  │
                       │  个人额度记录  │
                       └──────────────┘
                              │
                              │N
                              ▼
┌──────────────┐       ┌──────────────┐       ┌──────────────┐
│   AiTool     │1────N │  AiModel     │1────N │UsageRecord   │
│   AI 工具     │       │ 模型/套餐/费率 │       │  消费记录     │
└──────────────┘       └──────────────┘       └──────────────┘
                              │                       │
                              │                       │N
                              ▼                       ▼
                       ┌──────────────┐       ┌──────────────┐
                       │  ToolRate    │       │QuotaApplication│
                       │   费率配置     │       │   额度申请     │
                       └──────────────┘       └──────────────┘
```

### 6.2 核心实体定义

#### 6.2.1 Company（公司预算）
| 属性 | 说明 |
|---|---|
| company_id | 主键 |
| fiscal_year | 财年 |
| total_budget_cny | 年度总预算（人民币本位币） |
| total_budget_usd | 年度总预算（美元） |
| exchange_rate | 默认汇率 |
| created_at / updated_at | 时间戳 |

#### 6.2.2 Department（部门）
| 属性 | 说明 |
|---|---|
| dept_id | 主键 |
| company_id | 所属公司 |
| dept_name | 部门名称 |
| monthly_budget_cny | 月度预算 |
| manager_id | 部门主管用户 ID |

#### 6.2.3 User（用户）
| 属性 | 说明 |
|---|---|
| user_id | 主键 |
| dept_id | 所属部门 |
| username | 用户名 |
| email | 邮箱 |
| role | 角色：employee / manager / admin |
| tool_accounts | 各工具账号映射（JSON） |

#### 6.2.4 AiTool（AI 工具）
| 属性 | 说明 |
|---|---|
| tool_id | 主键 |
| tool_name | 工具名称，如 GitHub Copilot |
| billing_mode | 计费模式：seat / token / credit / mixed |
| currency | 结算币种：CNY / USD |
| sync_type | 同步方式：api / file / manual |

#### 6.2.5 AiModel（模型/套餐）
| 属性 | 说明 |
|---|---|
| model_id | 主键 |
| tool_id | 所属工具 |
| model_name | 模型或套餐名，如 GPT-4o、Claude 3.5 |
| unit_price | 单价 |
| unit | 计量单位：token、credit、seat_month |
| tiered_pricing | 阶梯价配置（JSON） |

#### 6.2.6 QuotaRecord（额度记录）
| 属性 | 说明 |
|---|---|
| quota_id | 主键 |
| user_id | 所属用户 |
| dept_id | 所属部门 |
| period | 所属周期，如 2026-08 |
| quota_type | 类型：monthly_base / additional |
| total_amount | 额度总额 |
| used_amount | 已用金额 |
| remaining_amount | 剩余金额 |
| source_application_id | 关联申请 ID |

#### 6.2.7 UsageRecord（消费记录）
| 属性 | 说明 |
|---|---|
| usage_id | 主键 |
| user_id | 消费用户 |
| dept_id | 消费部门 |
| tool_id | 工具 |
| model_id | 模型 |
| usage_date | 消费日期 |
| usage_quantity | 用量 |
| original_amount | 原始金额 |
| original_currency | 原始币种 |
| amount_cny | 折算人民币金额 |
| source | 数据来源：api / import / manual |

#### 6.2.8 QuotaApplication（额度申请）
| 属性 | 说明 |
|---|---|
| application_id | 主键 |
| applicant_id | 申请人 |
| dept_id | 部门 |
| type | 类型：base / additional |
| amount | 申请金额 |
| reason | 用途说明 |
| status | 状态：pending / manager_approved / admin_approved / rejected |
| approver_id | 审批人 |
| approve_comment | 审批意见 |
| created_at / updated_at | 时间戳 |

#### 6.2.9 AlertRule / AlertLog（预警规则与日志）
| 属性 | 说明 |
|---|---|
| rule_id | 主键 |
| target_type | 对象：company / department / user |
| target_id | 对象 ID |
| threshold_pct | 阈值百分比 |
| notify_roles | 通知角色 |
| log_id / triggered_at | 触发记录 |

---

## 7. 关键业务流程

### 7.1 月度预算初始化流程
1. 预算管理员在系统中配置公司年度总预算，并拆分为月度预算。
2. 系统按部门配置，将月度预算分配至各部门。
3. 部门主管收到预算通知，将部门月度预算拆分为员工基础额度。
4. 系统生成 QuotaRecord，个人额度生效。

### 7.2 日常消费扣减流程
1. 系统按配置周期同步各 AI 工具账单。
2. 将原始账单解析为 UsageRecord，并折算为人民币。
3. 根据消费用户归属，更新个人 QuotaRecord 的 used_amount 与 remaining_amount。
4. 同步更新部门预算已用金额与公司总预算已用金额。
5. 校验预警规则，触发通知。

### 7.3 额度追加流程
1. 员工发起追加申请，系统预校验部门剩余预算。
2. 申请提交至部门主管审批；若超额或超阈值，升级至预算管理员。
3. 审批通过后，系统新增 additional 类型 QuotaRecord，并更新部门剩余预算。
4. 员工收到通知，可继续使用。

---

## 8. 非功能需求

### 8.1 性能
- 页面加载主要看板数据 ≤ 3 秒（当前 8 款工具、百人规模）。
- 数据同步任务支持并发，单工具账单 ≤ 5000 条时 5 分钟内完成。

### 8.2 可靠性
- 账单同步失败需记录并告警，不影响已同步数据的展示。
- 关键操作（审批、预算调整）记录审计日志，不可物理删除。

### 8.3 安全
- 角色权限控制：员工只能查看个人数据，主管只能查看本部门数据，预算管理员可查看全局数据。
- 敏感操作需登录态校验，密码/Token 加密存储。
- MVP 阶段暂不做 SSO，但预留接入企业微信/钉钉/飞书账号的字段。

### 8.4 易用性
- 看板使用图表化展示（柱状图、饼图、折线趋势）。
- 操作路径清晰：员工 3 步内发起申请；主管 2 步内完成审批。

### 8.5 扩展性
- 工具/模型/费率表独立维护，新增工具只需配置同步规则与费率，无需改代码。
- 预警规则可配置阈值与通知对象。
- 币种与汇率独立配置，支持未来多币种场景。

---

## 9. 需求闭环自证

### 9.1 三类角色需求是否都被满足？
| 角色 | 核心诉求 | 对应功能 | 是否满足 |
|---|---|---|---|
| 员工 | 看自己这个月花了多少、还剩多少、能申请 | 个人总览、工具明细、剩余额度、申请/追加、预警通知 | ✔ |
| 部门主管 | 看部门预算、已用、剩余、模型分布、成员排名 | 部门看板、模型分布、成员排名、审批、额度分配 | ✔ |
| 预算管理员 | 看全公司总预算、各部门使用率、对比、排行、预警 | 全局看板、部门对比、模型排行、预警配置、报表导出 | ✔ |

### 9.2 功能之间是否形成完整操作链？
- 预算配置 → 部门拆分 → 个人额度 → 消费同步 → 额度扣减 → 预警 → 追加申请 → 审批 → 额度更新 → 月末复盘，形成闭环。
- 任一环节均可向前追溯：消费记录可追溯至工具账单与汇率配置；额度申请可追溯至审批人与预算来源。

### 9.3 需求能否指导逻辑模型设计？
- 已明确 9 个核心实体及其关系，可直接映射为数据库表结构。
- 已明确关键字段（金额、币种、汇率、用量、状态、审批流），可指导字段类型与索引设计。
- 已明确预算层级、额度类型、审批状态、预警对象等业务枚举，可指导代码枚举定义。

### 9.4 是否避免了与核心能力无关的功能？
- 本 MVP 聚焦“预算-额度-消费-预警-审批”核心闭环。
- 不引入：AI 工具实际调用网关、实时拦截、复杂的组织架构、SSO 登录、多租户、发票报销、供应商付款等功能，避免范围蔓延。

---

## 10. MVP 范围与优先级

### 10.1 P0（必须有，支撑核心闭环）
1. 公司/部门/个人预算与额度配置。
2. 员工个人消费总览与明细。
3. 部门主管看板、成员排名、模型分布。
4. 额度申请与审批流程。
5. 消费数据手动导入与自动同步（至少接入 2-3 款核心工具）。
6. 超预算/超额度的站内消息预警。

### 10.2 P1（重要，提升体验）
1. 全公司全局看板与部门对比。
2. 模型费用排行榜。
3. 报表导出（Excel/PDF）。
4. 审批升级规则配置。
5. 多币种与汇率配置。

### 10.3 P2（后续迭代）
1. 更多 AI 工具自动接入。
2. 邮件/钉钉/飞书通知渠道。
3. 预算执行率预测。
4. 额度季度结转、项目制预算。

---

## 11. 风险与假设

| 风险/假设 | 说明 | 应对策略 |
|---|---|---|
| 工具无开放 API | 部分工具无法自动拉取账单 | 先支持 CSV/Excel 导入；对账差异由管理员处理 |
| 用户归属不明确 | 账单中缺少员工标识 | 建立工具账号映射表；无法归属时暂放部门公共池 |
| 汇率波动 | 美元工具受汇率影响 | 提供固定汇率配置，月末可调整并记录 |
| 实时性差 | 多数工具账单 T+1 出账 | MVP 采用事后同步+预警，不做实时拦截 |
| 超支无法硬控 | 工具侧不支持额度封顶 | 通过审批+预警+主管问责实现软控 |

---

## 12. 附录：术语表

| 术语 | 说明 |
|---|---|
| 预算（Budget） | 公司或部门在某一周期内可支配的 AI 费用上限 |
| 额度（Quota） | 分配给员工个人在某一周期内可使用的 AI 费用上限 |
| 消费（Usage） | 员工使用 AI 工具产生的实际费用记录 |
| 追加额度（Additional Quota） | 月度基础额度用尽后，通过审批额外增加的额度 |
| 工具账号映射 | 将各 AI 工具账号与公司员工关联起来的配置 |
| 公共池 | 无法明确归属到个人的消费，暂时归入部门公共消费 |

---

**文档结束**
